import { z } from 'zod';
import { MESSAGES } from '@/constants/messages';
import { FIELDS } from '@/constants/fields';

/**
 * @schema create{{pascalCase name}}Schema
 * @description Validates incoming payload for creating a new score event.
 * 
 */
export const create{{pascalCase name}}Schema = z.object({
  body: z.object({
    // TODO: Declare validation here (EG: name: z.string().min(1, MESSAGES.COMMON.VALIDATION.REQUIRED(FIELDS.NAME))
  }),
});

/**
 * @schema update{{pascalCase name}}Schema
 * @description Validates partial payloads for update.
 */
export const update{{pascalCase name}}Schema = z.object({
  body: z.object({
    // TODO: Declare validation here (EG: name: z.string().min(1, MESSAGES.COMMON.VALIDATION.REQUIRED(FIELDS.NAME)).optional())
  }),
  params: z.object({
    id: z
      .string({ message: MESSAGES.VALIDATION.REQUIRED(FIELDS.ID) })
      .trim()
      .uuid({ message: MESSAGES.VALIDATION.INVALID_FORMAT(FIELDS.ID) }),
  })
});

export type Create{{pascalCase name}}DTO = z.infer<typeof create{{pascalCase name}}Schema>['body'];
export type Update{{pascalCase name}}DTO = z.infer<typeof update{{pascalCase name}}Schema>['body'];